apiVersion: mpas.ocm.software/v1alpha1
kind: Repository
metadata:
  name: test-repository
spec:
  credentials:
    secretRef:
      name: git-secret
  owner: e2e-tester
  provider: gitea
  domain: http://gitea.ocm-system.svc.cluster.local:3000
  repositoryName: test # this must match with the repository created in the test.
